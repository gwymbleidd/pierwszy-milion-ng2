import { Route } from '@angular/router';

import { BuildingsComponent } from './index';

export const BuildingsRoutes: Route[] = [
  	{
    	path: 'buildings',
    	component: BuildingsComponent
  	}
];
